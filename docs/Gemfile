# frozen_string_literal: true

# Gemfile for the documentation site (GitHub Pages, Jekyll + Just the Docs).
# Separate from the gem's own Gemfile so the docs build doesn't pull Rails.
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.10"
gem "jekyll-remote-theme", "~> 0.4"
