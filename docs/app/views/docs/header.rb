# frozen_string_literal: true

module Views
  module Docs
    # A doc page header (eyebrow + title + lead). Provided by docs-kit
    # (::Docs::Header). This subclass keeps the historical `Views::Docs::Header`
    # name.
    class Header < ::Docs::Header
    end
  end
end
