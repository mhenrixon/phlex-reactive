# frozen_string_literal: true

module Views
  # An inline lucide SVG. Provided by docs-kit (Docs::Icon). This subclass keeps
  # the historical `Views::Icon` name used across the docs and reactive components.
  class Icon < ::Docs::Icon
  end
end
